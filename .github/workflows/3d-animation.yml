name: Update 3D Animation
on:
  schedule:
    - cron: '0 0 * * 0' 
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Update animation data
        run: |
          # Script para atualizar dados da animação
          echo "Atualizando estatísticas..."
      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "🎨 Update 3D animation data" || exit 0
          git push